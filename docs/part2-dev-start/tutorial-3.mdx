---
sidebar_position: 5
---

# 案例三：构建你的第一个 FHE 应用

## 官方示例

本案例使用 **TFHE-rs**（Rust 实现），由 Zama 提供。

| 资源 | 链接 |
|------|------|
| TFHE-rs 快速开始 | [docs.zama.ai/tfhe-rs/get-started/quick-start](https://docs.zama.ai/tfhe-rs/get-started/quick-start) |
| TFHE-rs GitHub 示例 | [github.com/zama-ai/tfhe-rs/tree/main/examples](https://github.com/zama-ai/tfhe-rs/tree/main/examples) |
| TFHE-rs 配置文档 | [docs.zama.ai/tfhe-rs/configuration/rust-configuration](https://docs.zama.ai/tfhe-rs/configuration/rust-configuration) |

## 安装和环境

详见：[TFHE-rs 安装文档](https://docs.zama.ai/tfhe-rs/get-started/installation)

```bash
# 安装 Rust
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
source ~/.cargo/env

# 创建项目
cargo new tfhe-example
cd tfhe-example

# 添加依赖
echo 'tfhe = { version = "~1.6.1", features = ["integer"] }' >> Cargo.toml

# 运行
cargo run --release
```

## 官方示例代码

来源：[TFHE-rs Quick Start](https://docs.zama.ai/tfhe-rs/get-started/quick-start)

```rust
use tfhe::{ConfigBuilder, generate_keys, set_server_key, FheUint8};
use tfhe::prelude::*;

fn main() {
    let config = ConfigBuilder::default().build();

    // 客户端：生成密钥
    let (client_key, server_key) = generate_keys(config);

    let clear_a = 27u8;
    let clear_b = 128u8;

    // 客户端：加密数据
    let a = FheUint8::encrypt(clear_a, &client_key);
    let b = FheUint8::encrypt(clear_b, &client_key);

    // 服务端：在密文上计算
    set_server_key(server_key);
    let result = a + b;

    // 客户端：解密结果
    let decrypted_result: u8 = result.decrypt(&client_key);

    let clear_result = clear_a + clear_b;
    assert_eq!(decrypted_result, clear_result);
}
```

## 下一步

1. 查看 [TFHE-rs 完整示例](https://github.com/zama-ai/tfhe-rs/tree/main/examples)
2. 学习 [TFHE-rs 配置文档](https://docs.zama.ai/tfhe-rs/configuration/rust-configuration) 优化性能
3. 参考 [fhEVM 教程](https://docs.zama.ai/protocol/solidity-guides/getting-started/quick-start-tutorial) 构建区块链隐私应用

## 常见问题

| 问题 | 解决方案 |
|------|----------|
| 编译太慢 | 使用 `cargo build --release` |
| 内存不足 | 减小参数或使用 Docker |
| 精度不够 | 选择更大的参数或 CKKS 方案 |

更多资源：

- [OpenFHE Examples](https://github.com/openfheorg/openfhe-development)
- [Microsoft SEAL Examples](https://github.com/microsoft/SEAL)
